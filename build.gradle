apply plugin: 'groovy'
apply plugin: 'idea'

version = '0.1.1-SNAPSHOT'
group = 'com.nola.gradle.plugins'

repositories {
    mavenCentral()
}

uploadArchives {
    repositories {
       flatDir(dirs: file('repos'))
    }
}

dependencies {
	 groovy group: 'org.codehaus.groovy', name: 'groovy', version: '1.7.8' 
	compile gradleApi()
}